@model IEnumerable<BudgetManager_V1.Models.IncomeExpenseGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<div class="container">

    <table class="table">
        <tr>
            <th>
                @Html.ActionLink("Cash flow type", "Index", new { sortWay = ViewBag.CashFlow })
                @*@Html.DisplayNameFor(model => model.CashFlow.Type)*@
            </th>
            <th>
                @Html.ActionLink("Name", "Index", new { sortWay = ViewBag.Name })
                @*@Html.DisplayNameFor(model => model.Name)*@
            </th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CashFlow.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    </table>
</div>


<div class="container">
    @Html.ActionLink("Back to Db tables list", "Index", "DbTablesAdmin")
</div>
